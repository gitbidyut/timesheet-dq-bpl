version: 0.2
phases:
  install:
    commands:
      - echo "hello"
  build:
    commands:
      - echo "Starting SageMaker Pipeline execution"
      - aws sagemaker start-pipeline-execution  --pipeline-name timesheet-dq-dev
